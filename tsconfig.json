{
  "compilerOptions": {
    "outDir": "app/dist/js", //digo para onde que os arquivos que serão compilados para js irão
    "target": "ES6", //digo qual é a versão do javascript que será gerada ao compilar
    "noEmitOnError": true, //não permite que o typescript gere arquivos js caso haja algum erro no arquivo ts
    "noImplicitAny": true, //para que o types não adote o tipo any automaticamente quando alguma variável não for tipada
    "removeComments": true, //remove os comentários na hora de compilar o código para js
    "strictNullChecks": true,
    "experimentalDecorators": true //para que eu consiga usar decorators na aplicação
  },
  //digo o caminho que os arquivos que serão lidos pelo typescript estão. aqui digo que dentro de app, são todas as subpastas e qualquer arquivo
  "include": ["app/src/**/*"]
}
